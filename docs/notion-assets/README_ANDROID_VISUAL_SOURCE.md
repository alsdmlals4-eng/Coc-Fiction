# Android visual source

The approved COC-Fiction dashboard must be displayed from an actual image asset, not from status text. The current legacy `COC-Fiction_Approved_Visual_Preview.jpg` is only a 180×120 transport thumbnail and is not sufficient as the human-facing visual.
