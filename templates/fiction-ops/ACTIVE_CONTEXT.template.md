# Fiction Active Context

> 현재 상태와 다음 작업만 압축한다. 작품 코어·설정·플롯 전문을 복제하지 않는다.

## 기준

- 프로젝트:
- 현재 브랜치·PR:
- 기준 main commit:
- Resume Rule: `FETCH_LATEST_MAIN_BEFORE_USE`
- Manuscript Stage: `DISCOVER / OUTLINE / DRAFT / REVISE / POLISH`
- 현재 Work Mode: `PLAN / BUILD / REVIEW`
- 현재 책임 원본:

## 현재 목표

한 문장으로 적는다.

## 확정·구현·검증 상태

- 확정:
- 작성 완료:
- 검증 완료:
- 미검증:
- 충돌·결정 대기:

## 외부 산출물 / repository 승격 상태

외부 파일이 GitHub production manuscript와 별도 lifecycle을 가질 때만 사용한다.

```yaml
artifact:
  filename:
  coverage:
  sha256:

delivery_state: DRAFT | QA_VERIFIED | DELIVERED
repository_promotion_state: NOT_REQUIRED | PENDING | PARTIAL | PROMOTED

repository:
  topology:
  promotion_target:
  verified_prefix_or_scope:
  migration_boundary:
  legacy_tail_or_debt:
  validation_gate:
```

`QA_VERIFIED`/`DELIVERED`는 외부 산출물의 상태이며 자동으로 `PROMOTED`를 뜻하지 않는다.

## 이번 작업 범위

- 포함:
- 제외:
- 반드시 보존:
- 겹치는 다른 브랜치·파일:

## 다음 정확한 작업

1.
2.
3.

## 위험·재개 조건

- 위험:
- 차단 원인:
- 다음 작업자가 먼저 읽을 파일:
- 다음 명령·행동:
